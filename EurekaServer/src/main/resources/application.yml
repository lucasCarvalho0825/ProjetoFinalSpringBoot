server:
  port: 8081

spring:
  application:
    name: 'EurekaServer'

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: 'http://localhost:8081/eureka'